const mongoose = require("mongoose");
const db = require("./connection");


constTodoSchema = mongoose.Schema({
    
  });

  constTodoModel = mongoose.model("Todos",TodoSchema);

module.exports =TodoModel;